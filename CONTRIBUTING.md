# ZFL (Zsh Function Library) 社区贡献指南

欢迎参与 **ZFL (Zsh Function Library)** 的建设！ZFL 是一个面向高性能、模块化的 Zsh 配置与函数库。为了让整个项目保持极速启动、运行稳定且易于维护，所有提交给社区的 Zsh 脚本都应严格遵守以下开发规范。

---

## 🛠️ 1. 本地开发环境设置

1.  **克隆与安装**：
    将本项目克隆或软链接到 `~/.config/zsh`，并在 `~/.zshrc` 尾部引入 `source ~/.config/zsh/base.zsh`。
2.  **隔离开发**：
    *   本地测试与私有临时脚本**请勿**直接放入 `functions/`。
    *   请放入根目录下的 **`custom_functions/`** 文件夹中。该目录已被 `.gitignore` 忽略，不会影响 Git 提交与同步。
    *   框架启动时会自动扫描并加载 `custom_functions/` 下的脚本。

---

## 📝 2. 代码开发规范 (核心要求)

为了确保贡献的质量，我们编写了静态分析工具（`zfl lint`）。任何合并进主分支的 PR 都会在 CI 中运行 Lint 校验。以下是您在编写脚本时必须遵守的规则：

### 1) 文件名与主入口函数 1:1 映射
*   文件名如果是 `foo.zsh`，文件内**必须且仅能**定义一个与文件名同名的全局主函数 `foo()`。
*   例如，在 `functions/weather.zsh` 中定义 `weather() { ... }`。

### 2) 标准元数据头 (Metadata Header)
为了能被 `zfl` 命令行工具自动识别并提取文档说明，请在脚本文件最顶部添加以 `#?` 开头的标准元数据说明：
```zsh
#? 名称: weather
#? 描述: 终端快速查询实时天气与天气预报
#? 作者: Royi
#? 版本: 1.0.0
#? 依赖: curl
#? 用法: weather [城市名称]
#? 示例: weather beijing
```

### 3) 变量必须局部化 (`local`)
*   函数体内部的临时变量、循环变量和输入读取变量**必须**显式使用 `local` 或 `typeset` 声明，防止污染用户 Shell 的全局命名空间。
*   **违规示例**：`temp_val="123"` 或 `for x in a b; do ...`（没有 `local x`）。
*   **正确示例**：`local temp_val="123"` 或 `local x; for x in a b; do ...`。

### 4) 辅助函数命名空间与作用域隔离
*   **嵌套定义（首选）**：如果有内部辅助逻辑，推荐将辅助函数嵌套定义在主入口函数内部。
*   **全局命名与清理**：如果必须定义在文件最外层（不推荐），其名称必须以 `_主函数名_` 或 `_主函数名`（自动补全代理函数）开头，且在主函数退出前执行 `unfunction` 进行销毁。例如：
    ```zsh
    weather() {
        # 嵌套定义辅助函数
        _weather_helper() { ... }
        
        _weather_helper "$@"
        
        # 退出前清理
        unfunction _weather_helper
    }
    ```

### 5) 避免硬编码 ANSI 颜色字符
*   严禁在脚本中硬编码 ANSI Escape 色彩转义序列（如 `\033[31m`）。
*   应当在函数体内使用 `load_color RED GREEN RESET` 动态载入预设颜色变量，再进行终端渲染。

### 6) 后台进程 FD 3 安全重定向
*   由于 ZFL 启动流通过系统描述符 3 (`exec 3< ...`) 隔离运行，任何会 fork 执行的后台命令（如 `cmd &` 或 `coproc`）中，**必须关闭描述符 3**（即使用 `3<&-` 重定向），否则会导致父终端异常锁死。
*   **正确示例**：`my_daemon_job >/dev/null 2>&1 3<&- &`。

### 7) 外部命令依赖检测
*   若您的脚本依赖外部 CLI 工具，推荐在函数入口使用 `zfl_require` 进行依赖断言保护：
    ```zsh
    weather() {
        # 声明并校验依赖，若缺失将抛出友好报错并以 1 状态码退出
        zfl_require curl || return 1
        ...
    }
    ```

### 8) Python 辅助与副产物规范
*   **定位约束**：`python/` 目录下只允许编写用于辅助 `functions/` 业务函数的跨语言 Python 脚本。
*   **缓存隔离**：所有在运行期间由脚本自动生成的个性化副产物（例如翻译结果、下载缓存、临时状态等），**必须统一存放在 `~/.cache/zsh/`** 目录下。严禁直接写入或污染全局技能目录（`~/.agents/skills/`）或其他非暂存的代码路径，以防干扰 Git 工作流与版本更新。

---

## 🔍 3. 提交 PR 前的本地自检

在向仓库提交 Pull Request 前，请务必在本地终端中运行 `zfl lint` 对您的代码进行检测：

1.  **检测特定函数**：
    ```bash
    zfl lint <函数名>
    ```
2.  **检测全量代码**：
    ```bash
    zfl lint
    ```
3.  **验收标准**：
    *   确保输出为 **`[✓] 完美通过！未检测到任何代码规范问题。`**。
    *   确保本地运行退出码为 `0`（可通过 `echo $?` 确认）。如果存在任何 Warning（警告）或 Error（错误），GitHub Actions 门禁将自动拒绝 PR 合并。
